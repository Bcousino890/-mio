#!/usr/bin/env node
// Extractores de fotos específicos para cada plataforma CRM
// Este archivo será completado con implementaciones específicas por agente

export async function extractPhotosFromCRM(crm, html, agencyDomain, referenceId) {
  // Placeholder: será implementado específicamente para cada CRM
  // por el agente de CRM Photo Parsers
  return []
}

export async function extractPhotosFromMobilia(html, baseUrl) {
  return []
}

export async function extractPhotosFromInmoweb(html, baseUrl) {
  return []
}

export async function extractPhotosFromLevel(html, baseUrl) {
  return []
}

export async function extractPhotosFromFotocasa(html, baseUrl) {
  return []
}
