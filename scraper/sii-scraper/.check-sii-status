Archivo centinela: cada push a este archivo (en main) dispara
.github/workflows/check-sii-status.yml, que SSHea al VPS e imprime el estado
del scrape SII mapasui (lockfile, procesos, logs, output y predios en BD).
De solo lectura: no toca el scrape en curso.

Historial de consultas:
- 2026-07-07 — primera consulta: la pantalla /chile/sii-mapasui muestra 0
  predios ingestados 3 días después del lanzamiento de Las Condes (manzanas-geo).
- 2026-07-07 — segunda consulta, post-relanzamiento (PR #114, run scrape-sii-mapasui #2
  a las 14:32 UTC): verificar que el proceso relanzado sigue vivo y avanzando en
  manzanas-geo tras el fix del rsync que le borraba output/venv.
- 2026-07-08 — tercera consulta (~12h después del relanzamiento de las 14:32
  UTC del 7 de julio): el usuario reporta que /chile/sii-mapasui sigue en 0
  predios; verificar cuántas manzanas lleva descubiertas manzanas-geo para
  decidir si la arquitectura de ingesta al final de la comuna es viable o
  hay que cambiarla a incremental.
- 2026-07-11 — post modo-cola (#132) y relanzamiento determinista: el dashboard
  sigue en 1 comuna (Las Condes, 80.611). Verificar que el proceso corre el
  run-sii-mapasui.sh NUEVO (modo cola), en qué comuna/etapa va, si ya existe
  output/.complete-15108 y si arrancó el descubrimiento de manzanas de Vitacura
  (15160). Confirmar que va a avanzar a Vitacura, no que quedó re-corriendo
  Las Condes.
- 2026-07-12 — validación del RE-SCRAPE limpio (grilla 40 m + sondeo por vacíos,
  lanzado 01:14 tras reset): confirmar que el proceso está vivo con el pipeline
  nuevo, en la etapa manzanas-geo de Las Condes a 40 m, y que output/manzanas/
  las_condes.jsonl ya trae MÁS manzanas que las 1.099 de la grilla vieja de
  100 m (señal de que el fix de cobertura funciona).

- 2026-07-12 (val) — validación post-fix OOM: confirmar proceso vivo con el
  pool acotado, memoria estable (no cerca de OOM), y manzanas de Las Condes
  subiendo a 40 m (>1.099 = fix de cobertura andando). Sin diag zombi.

- 2026-07-12 (enum) — validación post-reset a enumeración de manzanas (reset
  01:37, watchdog relanzó 01:47): confirmar que el proceso vivo corre la etapa
  'manzanas' (enumeración IDs 1-4000, probe_depth=20) para Las Condes —no la
  grilla geográfica—, cuántas manzanas lleva output/manzanas/las_condes.jsonl
  (objetivo ~3.400 vs las ~1.099 de la grilla de 100 m) y si ya arrancó la
  etapa de predios / la ingesta incremental a la BD.
- 2026-07-12 (enum2) — validar enumeración andando: manzanas subiendo rápido.
- 2026-07-12 (enum3, ~09:00 UTC) — chequeo de ritmo: a las 05:51 el JSONL de
  manzanas llevaba 1.548 líneas con 429 casi continuo (renovación de sesión
  cada ~10 s). Medir cuántas manzanas hay ahora (¿avance real o estancado por
  el WAF?), si la enumeración terminó y arrancó la etapa de predios (conteo en
  sii_mapasui_predios_cl sobre el baseline de 80.611), y que siga sin zombis
  manzanas-geo y con memoria estable.
- 2026-07-12 (enum3-bis) — mismo chequeo re-disparado tras añadir el paso que
  publica la salida en la rama sii-status (la API de Actions no siempre está
  disponible desde las sesiones; con la rama, el estado se lee con git fetch).
- 2026-07-12 (sesiones) — validar el relanzamiento con SII_SESSIONS=6: antes
  (1 sesión) el conteo de manzanas quedó estancado ~3h (1.548→1.550) bajo 429
  continuo. Confirmar que ahora hay ~6 procesos de sesión activos y que
  las_condes.jsonl sube claramente más rápido que esa tasa (~1 manzana/3h).
